name: Restore - Wait for X minutes

on:
  workflow_dispatch:
    inputs:
      WAIT_MINUTE:
        description: minutes wait
        default: 5
        type: number

jobs:
  wait-time:
    runs-on: ubuntu-latest
    steps:
      - run: sleep $(${{ inputs.WAIT_MINUTE }} * 60)
