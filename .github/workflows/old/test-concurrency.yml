name: Test concurrency group

on:
  workflow_dispatch: {}

concurrency:
  group: ${{ github.run_id }}
  cancel-in-progress: true

jobs:
  test-concurrency:
    name: Test concurrency
    runs-on: ubuntu-latest
    steps:
      - run: sleep 120
      - run: echo GHI
